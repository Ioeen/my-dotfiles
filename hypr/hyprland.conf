monitor=,preffered,auto,1
monitor= DP-2, 1920x1080@200, 0x0, 1
$terminal = kitty
$browser = firefox

$mainMod = SUPER

bind = $mainMod, C, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, V, togglefloating,

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, B, exec, $browser
bind = $mainMod, SPACE, exec, rofi -show drun
bind = $mainMod, TAB, exec, rofi -show window
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6

bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6

input {
    kb_layout = de                    # Setzt das Tastaturlayout auf Deutsch
    follow_mouse = 1
}

general {
    gaps_in = 5                       # Abstand zwischen Fenstern
    gaps_out = 10                     # Abstand zum Bildschirmrand
    border_size = 2
    col.active_border = rgba(ff6b00ee) rgba(ff6b00ee) 45deg
    col.inactive_border = rgba(595959aa)
    layout = dwindle
}

decoration {
    rounding = 10                     # Abgerundete Ecken für Fenster
}

misc {
disable_hyprland_logo = true
disable_splash_rendering=true
force_default_wallpaper = 0
}

bind = $mainMod, T, exec, hyprctl notify 1 3000 "rgb(ff6600)" "fontsize:20 $(date +'%H:%M  -  %A, %B %d')"
binde = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
binde = , XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindl = , XF86AudioPlay, exec, playerctl play-pause
